<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.course.com.course.model">
    <select id="loginCase" parameterType="Integer" resultType="com.course.com.course.model.LoginCase">
        select * from loginCase
        where id = #{id}
    </select>
</mapper>
